---
- name: OpenVPN Server
  become: yes
  hosts: all
  tasks:

    - name: Install packages
      package:
        name:
          - openvpn
          - openssl
        state: present

    - name: Ip forwarding
      sysctl:
        name: net.ipv4.ip_forward
        value: '1'
        state: present

    - name: Copy CA files and config
      copy:
        src: '{{item}}'
        dest: '/etc/openvpn/'
        owner: root
        group: root
        mode: 0644
      loop:
        - ../easy-rsa/pki/ca.crt
        - ../easy-rsa/pki/dh.pem
        - ../easy-rsa/pki/issued/openvpn-server.crt
        - ../easy-rsa/pki/private/openvpn-server.key
        - ../server.conf
      notify:
        - Restart openvpn

    - name: Run genkey
      shell:
        chdir: /etc/openvpn/
        cmd: openvpn --genkey > ta.key
        creates: /etc/openvpn/ta.key

    - name: enable openvpn
      service:
        name: openvpn@server
        enabled: yes
        state: started

# fail2ban etc / country block - UK only
# HBF?

  handlers:
    - name: Restart openvpn
      service:
        name: openvpn@server
        state: restarted
